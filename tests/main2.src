// Testing id nesting
func main() -> void {
    id(id).id;
    id(id).id();
    id.id[id];
    id[id].id;
    id[id].id[id];
    id.id[id][id];
    id[id][id].id;
    id[id][id].id[id][id];
    id(id).id[id];
    id(id).id[id][id];
}
