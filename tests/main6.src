func mul(a: integer, b: integer) -> integer {
    return (a * b);
}

func main() -> void {
    let a: integer;
    a = mul(5, 8);
    a = 0;
    while (a < 10) {
        a = a + 1;
    };
    write(a);
    read(a);
    write(a);
}
